# Sanitized Configurations

All config files intentionally omit passwords, WireGuard private keys, router/SMB credentials, public IPs, MAC addresses, or other identifying details and use placeholders instead.
